Sub GetCalendarItems()
    Dim outlookApp As Outlook.Application
    Dim outlookNamespace As Outlook.Namespace
    Dim calendarFolder As Outlook.Folder
    Dim items As Outlook.Items
    Dim item As Object
    
    Set outlookApp = CreateObject("Outlook.Application")
    Set outlookNamespace = outlookApp.GetNamespace("MAPI")
    Set calendarFolder = outlookNamespace.GetDefaultFolder(olFolderCalendar)
    Set items = calendarFolder.Items
    
    ' Sort items by start date
    items.Sort "[Start]", True
    items.IncludeRecurrences = True
    
    ' Loop through calendar items
    For Each item In items
        Debug.Print item.Subject & " - " & item.Start
    Next item
End Sub